import { View } from "react-native";

/** Pestaña central: el tab bar abre el sheet de creación en lugar de esta pantalla. */
export default function CreateTabPlaceholder() {
  return <View style={{ flex: 1, backgroundColor: "#000" }} />;
}
